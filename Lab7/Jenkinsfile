pipeline {

    agent {
        node {
            label 'DoroginNSIP312' 
        }
    }
    stages{
        stage('Qemu') {
            steps{
                echo 'Start qemu'
            }
        }
        stage('Auto') {
            steps{
                echo 'Start autotests'
            }

        }
        stage('WebUI') {
            steps{
                echo 'Start WebUI tests'
            }

        }
        stage('Locust') {
            steps{
                echo 'Start Locust tests'
            }
        }
    }
}